#include <vector>
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[n];
    for (size_t i = 0; i < n; i++)
    {
        cin >> a[n];
    }
    for (size_t i = 0; i < n; i++)
    {
        cout << a[i] << " "; 
    }
    system("Pause");
}
