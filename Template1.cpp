#include <iostream>
using namespace std;


template <typename T>
void nhap();

template <typename T>
T timmax(T a, T b, T c);

int main() {
    
}

template <typename T>
void nhap() {
    T a, b, c;
    cin >> a >> b >> c;
}

template <typename T>
T timmax(T a, T b, T c) {
    T _max = a;
    if (b > max)
    {
        _max = b; 
    }
    if (c > max)
    {
        _max = c; 
    }
    return _max;
    
}